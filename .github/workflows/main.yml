name: Build and Run Unit Tests

on: [push, pull_request]

jobs:
  linux:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Run Unit Tests
      run: dotnet test src/GbUtil.E2ETests/GbUtil.E2ETests.csproj
      shell: bash